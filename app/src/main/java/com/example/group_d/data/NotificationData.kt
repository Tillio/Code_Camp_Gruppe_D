package com.example.group_d.data

// this class contains the layout of the notification-data
data class NotificationData(
    // the title holds the title of the message
    val title: String,
    // the message holds the message-text
    val message: String
)